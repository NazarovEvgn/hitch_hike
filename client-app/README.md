# ХичХайк - Клиентское PWA приложение

Прогрессивное веб-приложение для поиска автосервисов с реальным статусом загруженности на карте 2GIS.

## Быстрый старт

### 1. Получите 2GIS API ключ

**ВАЖНО:** Для работы карты необходим API ключ от 2GIS.

**Подробная инструкция:** См. [docs/2gis_api_setup.md](../docs/2gis_api_setup.md)

**Кратко:**
1. Зарегистрируйтесь на https://dev.2gis.ru
2. Создайте демо-ключ в Platform Manager (бесплатно)
3. Скопируйте ключ

### 2. Настройте переменные окружения

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Откройте .env и замените placeholder на ваш ключ
# DGIS_API_KEY=ваш-настоящий-ключ-от-2gis
```

### 3. Установите зависимости

```bash
npm install
```

### 4. Запустите dev-сервер

```bash
npm run dev
```

Приложение откроется на http://localhost:9000 (или другом доступном порту)

---

## Структура приложения

```
client-app/
├── src/
│   ├── pages/
│   │   ├── MapPage.vue          # Главная страница с картой 2GIS
│   │   ├── FavoritesPage.vue    # Избранные сервисы
│   │   └── ErrorNotFound.vue    # 404
│   ├── layouts/
│   │   └── MainLayout.vue       # Основной макет (будущий)
│   ├── boot/
│   │   └── axios.js             # Настройка HTTP клиента
│   ├── router/
│   │   └── index.js             # Маршрутизация
│   └── css/
│       └── app.scss             # Глобальные стили
├── .env                         # Переменные окружения (не в git)
└── quasar.config.js             # Конфигурация Quasar
```

---

## Основные функции

### ✅ Реализовано (Phase 4 - в процессе):

- [x] Инициализация Quasar PWA проекта
- [x] Интеграция 2GIS MapGL карты
- [x] Панель фильтров по типу сервиса (автомойка, ремонт, шиномонтаж)
- [x] Карточка бизнеса с информацией и действиями
- [x] Маршрутизация (главная, избранное, 404)

### 🔄 В разработке:

- [ ] Загрузка списка бизнесов из backend API
- [ ] Отображение маркеров на карте с цветовой кодировкой (🟢🟡🟠)
- [ ] Клик по маркеру → показ карточки бизнеса
- [ ] Фильтрация маркеров по типу сервиса
- [ ] Функция "Позвонить" (tel: ссылка)
- [ ] Форма записи онлайн
- [ ] Избранное (localStorage)
- [ ] PWA установка и offline режим

---

## Технологии

- **Quasar Framework 2.x** - Vue 3 + PWA
- **Vue 3** - Composition API
- **2GIS MapGL** - Интерактивная карта
- **Axios** - HTTP клиент для API
- **Vue Router** - Маршрутизация
- **Pinia** - State management (планируется)

---

## Скрипты

```bash
# Разработка
npm run dev              # Запуск dev-сервера с hot-reload

# Production
npm run build            # Сборка для production (PWA)

# Код-стиль
npm run lint             # Проверка ESLint
npm run format           # Форматирование кода
```

---

## Конфигурация

### Переменные окружения (.env):

```env
# 2GIS API Key (обязательно!)
DGIS_API_KEY=ваш-ключ-от-2gis
```

### API Endpoint:

Backend API URL настраивается в `quasar.config.js`:
```javascript
build: {
  env: {
    API_URL: process.env.API_URL || 'http://127.0.0.1:8000/api/v1'
  }
}
```

---

## Troubleshooting

### Карта не загружается (бежевый фон)

**Причина:** Отсутствует или неправильный API ключ от 2GIS.

**Решение:** См. [docs/2gis_api_setup.md](../docs/2gis_api_setup.md)

### Порт 9000 занят

Dev-сервер автоматически выберет другой порт (9001, 9003 и т.д.). Проверьте вывод в консоли.

### CORS ошибки

Убедитесь, что backend запущен и настроен для разрешения запросов с клиентского порта:
- Backend: `backend/.env` → `ALLOWED_ORIGINS=http://localhost:9000,...`

---

## Полезные ссылки

- [Быстрый старт для всей системы](../docs/quick_start.md)
- [Настройка 2GIS API](../docs/2gis_api_setup.md)
- [План разработки](../docs/dev_plan.md)
- [Документация Quasar](https://quasar.dev)
- [Документация 2GIS MapGL](https://docs.2gis.com/en/mapgl/overview)

---

**Версия:** Phase 4 - Client PWA (в разработке)
**Дата:** 30 ноября 2025
